1. make sure that you have install python by running the following command:
    ```
    python --version
    ```
2. pip install requests
3. pip install beautifulsoup4
4. 
   ```
   python data_collector.py
   ```